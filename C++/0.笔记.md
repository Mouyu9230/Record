### 3.2

#### string 操作相关

- `push_back`：追加单字符  
- `append`：追加一整串或数组  

#### 类型转换

- 数字字符串 → 数字：  
  `stoi()`、`stol()`、`stod()`、`stoll()`  
  （分别对应 `int`、`long`、`double`、`long long`）

- 数字 → 字符串：  
  `to_string()`


