{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run C++ (gdb)",
      "type": "cppdbg",
      "request": "launch",

      "program": "${workspaceFolder}/thread_pool", 
      "args": [],

      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",

      "environment": [],
      "externalConsole": false,

      "MIMode": "gdb",
      "miDebuggerPath": "/usr/bin/gdb",

      "setupCommands": [
        {
          "description": "Enable pretty-printing",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ],

      "preLaunchTask": "build"
    }
  ]
}